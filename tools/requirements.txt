# SPDX-License-Identifier: Apache-2.0
# Dependencies for migration tools in tools/
# Install with: pip install -r tools/requirements.txt
#
# NOTE: This is separate from requirements.txt (runtime lambdas) and
# requirements-lambda.txt (packaged lambda deps). These are operator-only tools.

boto3>=1.34
stripe>=8.0
moto[dynamodb,s3]>=5.0
