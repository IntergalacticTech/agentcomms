# AgentComms Console — environment variables
# Copy to .env.local for local development

# Primary API base URL
VITE_AGENTCOMMS_API_URL=https://api.agentcomms.dev/v1

# Legacy env var name still works locally if already configured
# VITE_API_URL=https://api.agentcomms.dev/v1
