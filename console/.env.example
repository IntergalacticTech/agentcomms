# AgentComms Console — environment variables
# Copy to .env.local for local development

# Primary API base URL (new name)
VITE_AGENTCOMMS_API_URL=https://api.victorymail.dev/v1

# Legacy fallback — still accepted during dual-run transition
# VITE_API_URL=https://api.victorymail.dev/v1
